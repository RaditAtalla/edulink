import { createMaterialTopTabNavigator } from "@react-navigation/material-top-tabs";

const Tab = createMaterialTopTabNavigator()

const NewsSlider = () => {
  return(
    <Tab.Navigator initialRouteName="Portal Beasiswa">

    </Tab.Navigator>
  )
}